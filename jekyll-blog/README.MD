# crgm.net jekyll blog

* `_config.yml` - the jekyll config
* my_src/ - jekyll blog
* pub/ - where generated html is output to


## notes

create a new blog run:

```
$ jekyll new crgmnet
$ rm -rf crgmnet/{_site,_posts}/
$ rm -f crgmnet/{404.html,about.md,index.md}
```
